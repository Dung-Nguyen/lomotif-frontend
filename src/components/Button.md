Button component example:

```js
<div>
  <Button
    className="mr-3"
    text="Click Me"
    onClick={() => alert('Button click')}
  />
  <Button
    className="mr-3"
    text="Outline"
    outline
    onClick={() => alert('Outline click')}
  />
</div>
```
